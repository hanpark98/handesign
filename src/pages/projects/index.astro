---
import Base from '@/layouts/Base.astro';
import Grid from '@/components/Grid.astro';
import ProjectCard from '@/components/ProjectCard.astro';

const projects = await Promise.resolve([
  { href: '/projects/example', title: 'Example Case Study', year: '2025', thumb: '/og-default.jpg', tags: ['Product', 'UX'] },
]);
---
<Base title="Projects — Han Park">
  <header class="pt-16 sm:pt-20">
    <h1 class="h1">Projects</h1>
    <p class="p mt-3 max-w-prose">Selected work across product design, branding, and interaction.</p>
  </header>
  <section class="py-10 sm:py-16">
    <Grid>
      {projects.map((p) => <ProjectCard {...p} />)}
    </Grid>
  </section>
</Base>
